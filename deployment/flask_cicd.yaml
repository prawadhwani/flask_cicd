apiVersion: v1
kind: Service
metadata:
  name: flask-cicd
  labels:
    app: flask-cicd
spec:
  type: NodePort 
  ports:
  - port: 8080 #this shouldn't matter
    protocol: TCP
    name: my-http-port
  selector:
    app: flask-cicd
    tier: backend
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-ingress
spec:
  backend:
    serviceName: flask-cicd
    servicePort: my-http-port
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: flask-cicd
  labels:
    app: flask-cicd
    tier: backend
spec:
  selector:
    matchLabels:
      app: flask-cicd
  replicas: 1
  template:
    metadata:
      labels:
        app: flask-cicd
        tier: backend
    spec:
      containers:
      - name: flask-cicd
        image: arcticf0x/flask_cicd
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: cont-port
        env:
        - name: FOR_GODS_SAKE_PLEASE_REDEPLOY
          value: 'THIS_STRING_IS_REPLACED_DURING_BUILD'
